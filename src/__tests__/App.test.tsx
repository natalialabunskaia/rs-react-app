import { it } from 'vitest'

//App reads saved searchTerm from localStorage on initial render
//App renders empty input when localStorage is empty
//App calls getPokemons on mount
//App shows loading state while request is pending
//App renders pokemon cards after successful service response
//App renders error message after rejected service response
//App trims/lowercases search term before saving
//App writes searchTerm to localStorage after search
//App does not repeat search when searchTerm equals localStorage value

it('render search component', () => {})

