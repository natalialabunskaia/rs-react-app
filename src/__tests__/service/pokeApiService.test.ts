import {it} from "vitest"
 
 it('', () => {});
// pokeApiService.getBySearchTerm('') calls getAllPokemons
// pokeApiService.getBySearchTerm('fire') calls getPokemonByType('fire')
// pokeApiService.getPokemonDescription normalizes API data
// axios/service rejected promise is handled in App